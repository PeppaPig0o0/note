### 豆瓣爬虫

```python
# requests库 安装
# 文件->设置->项目->Python解释器-> 点➕ -> 搜索requests -> 安装

# request.get(url) 向url发送一个get请求
# 响应状态码418，服务器发现我们是爬虫，不给响应

import requests
url = 'https://www.douban.com/'
response = requests.get(url)
print('响应类型：', type(response))
print('请求的地址：', response.url)
print('响应状态码：', response.status_code)
print('请求头：', response.headers)  # 爬虫默认的头部
print('响应内容：', response.text)
```

### GET请求传递参数

```python
import requests
# 使用get方法，通过url传递参数
url = 'https://www.douban.com/search'
params = {
    'q': '吃烧烤'
}
headers = {
    'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/105.0.0.0 Safari/537.36'
}
response = requests.get(url, params=params, headers=headers)
print('响应类型：', type(response))
print('请求的地址：', response.url)
print('响应状态码：', response.status_code)
print('响应内容：', response.text)
```

### POST请求传递表单数据

```python
import requests
# 使用post方法，通过传递表单参数
url = 'http://jw.lku.edu.cn/xtgl/login_slogin.html'
params = {
    'time': '1662601619000'
}
headers = {
    'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/105.0.0.0 Safari/537.36'
}
data = {
    'csrftoken': '47a777b4-f496-4500-9ff2-de25999dde40,47a777b4f49645009ff2de25999dde40',
    'language': 'zh_CN',
    'yhm': '20220219005',
    'mm': 'PayW7AtUmwPlIFpviWmhyKgl7ATYo7DDClsVyifcw5qcYhSRAMysF76CxpXgF7Uv6yA3T15WM7zuyKizGv6tWAqjXjEhk8gWaBleFYjvD8AbmFaWBW3IOa3wGJcPziYbTaEHewKlBiSWH65A6ZquAljrpsUx8W4y/mf/3BnK6pk=',
    'mm': 'PayW7AtUmwPlIFpviWmhyKgl7ATYo7DDClsVyifcw5qcYhSRAMysF76CxpXgF7Uv6yA3T15WM7zuyKizGv6tWAqjXjEhk8gWaBleFYjvD8AbmFaWBW3IOa3wGJcPziYbTaEHewKlBiSWH65A6ZquAljrpsUx8W4y/mf/3BnK6pk=',
}
response = requests.post(url, params=params, headers=headers, data=data)
print('响应类型：', type(response))
print('请求的地址：', response.url)
print('响应状态码：', response.status_code)
print('响应内容：', response.text)
```

